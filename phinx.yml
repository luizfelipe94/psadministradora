paths:
    migrations: ../db/migrations
    seeds: ../db/seeds

environments:
    default_migration_table: phinxlog
    default_database: development

    development:
        adapter: mysql
        host: localhost
        name: uberdatamanager3
        user: root
        pass: 
        port: 3306
        charset: utf8
